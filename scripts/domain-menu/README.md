Domain Menu

### remote-restore.sh

Restores website data from a remote backup archive. The script uses `sudo` automatically when run by a non-root user, so run it as root or ensure your user has sudo privileges.
